import * as React from "react";
import styled from "styled-components";
import {
  space,
  SpaceProps
} from "styled-system";
import {
  Icon
} from "../core";


export const IconButton = (props) => {
  return (
    <div></div>
  );
}
